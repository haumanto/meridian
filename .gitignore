# Dependencies
node_modules/

# Environment & secrets
.env
.env.*
!.env.example
.envrypt

# User config (contains RPC URL, wallet key, API keys)
user-config.json

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
hivemind-cache.json
telegram-offset.json
signal-weights.json

# Logs
logs/

# Claude-driven optimization reports (timestamped, user-specific)
optimization-reports/*.md
optimization-reports/latest-recommendations.json

# OS
.DS_Store

# Dev context
context.md
CONTEXT.md
dev-blocklist.json
docs/*
!docs/SAFETY.md

# Discord listener queue
discord-signals.json

# Playwright MCP browser logs
.playwright-mcp/

# Claude Code runtime lock
.claude/scheduled_tasks.lock

# Autoresearch: isolated profile data dirs + per-run results ledger
# (run configs under research/runs/*/config.json stay tracked)
profiles/
research/runs/*/results.jsonl

# One-off incident cleanup artifacts (test pollution remediation)
_incident_backup_*/
scripts/_incident_cleanup*.mjs
